"""
Este era un pequeño programa que hice para tantear números en
una operación de una tarea de algebra de la mis cucho
que era imposible ya que estaba mal planteado xD
"""
num= float(input("Número: "))

op1= (float(num)**float(num))

op2= (float(16)**float(num)**float(2))

print(op1)

print(op2)
